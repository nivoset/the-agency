{
  "name": "the-agency",
  "version": "1.0.0",
  "description": "CLI for hiring and resolving agency prompt repositories",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "bin": {
    "agency": "./dist/index.js"
  },
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\" && tsc -p tsconfig.json",
    "prepare": "pnpm build",
    "prepack": "pnpm build",
    "agency": "node dist/index.js",
    "local:test": "pnpm build && node dist/index.js --help && node dist/index.js agencies list",
    "test": "pnpm build && node dist/test/run.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.10.0",
  "dependencies": {
    "@types/node": "^25.5.0",
    "commander": "^14.0.3",
    "git": "^0.1.5",
    "typescript": "^6.0.2"
  }
}
