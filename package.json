{
  "name": "the-agency",
  "version": "1.0.2",
  "description": "CLI for hiring and resolving agency prompt repositories",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "bin": {
    "the-agency": "dist/index.js",
    "agency": "dist/index.js"
  },
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "tsc -p tsconfig.json",
    "prepare": "pnpm build",
    "prepack": "pnpm build",
    "the-agency": "node dist/index.js",
    "local:test": "pnpm build && node dist/index.js --help && node dist/index.js agencies list",
    "test": "pnpm build && node dist/test/run.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.10.0",
  "dependencies": {
    "@types/node": "^25.5.0",
    "commander": "^14.0.3",
    "git": "^0.1.5",
    "rimraf": "^6.1.3",
    "the-agency": "link:",
    "typescript": "^6.0.2"
  }
}
